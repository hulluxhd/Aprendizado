/* let notas = [1,2,4,8];
let valores = [1];
let soma = 0
// laço para exibir as notas e as somas na primeira posição
for(let i = 0; i < notas.length; i++){
    if(i+1 < notas.length)
        console.log(`${valores[i]} + ${notas[i+1]}`)
        valores.push(notas[i] + notas[i+1])   
    soma += notas[i]
}
// laço para exibir as notas de cada prova
console.log(soma/notas.length)
for(let i = 0; i < notas.length; i++){
    console.log(`Sua ${i+1}° nota foi ${notas[i]}`)
}
// exibição da nota final somada
console.log("Sua nota final foi " + soma/notas.length)
// situação do aluno
soma/notas.length < 7 ? console.log("Sua situação é: reprovado") : console.log("Sua situação é: aprovado")
console.log(Math.PI) */

